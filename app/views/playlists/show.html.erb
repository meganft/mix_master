<h1> <%= @playlist.name %></h1>

<ul>
  <% @playlist.songs.each do |song| %>
    <li> <%= link_to song.title, song %></li>
  <% end %>
</ul>

<h3><%= link_to "Edit", edit_playlist_path(@playlist) %></h3><br>

<h3><%= link_to "Back to Playlist Index", playlists_path %></h3><br>
